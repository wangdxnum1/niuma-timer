# 更新日志

本文件记录项目的所有重要变更。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 移除

- **提醒横幅与「休息 5 分钟」**——久坐 / 下班提醒不再弹应用内横幅，统一走系统通知（主窗可见与否不影响通道）；「知道了」与「休息 5 分钟」按钮随之移除：触发瞬间后端已重置起算点并进入冷却，ack 本就冗余，想歇只能托盘手动暂停（手动暂停链路不变）。同步删除 `pause_rest` / `remind_ack` 命令、`rest_secs` 状态字段与徽章「休息中」倒计时

### 新增

- **启动日志带构建信息（版本 / 编译时间 / git 提交 / 前端指纹）**——同一个版本号可以构建很多次，用户截图报问题时只看 `v1.2.0` 无法确认「装的是哪一次构建、哪个提交、哪份前端资源」。现由 `build.rs` 在**编译期**把四项注入 exe（`BUILD_TIME` 取本地时区、`BUILD_GIT` 跑 `git rev-parse --short HEAD`、`BUILD_FE_VER` 复用缓存戳指纹），`main.rs` 启动时在 `install_crash_log()` 之后、`.setup` 之前写一行 `build: v1.2.0 build=2026-09-23 18:00:00 git=abc1234 fe=xxxxxxxx` 到 `debug.log` 与 `panic.log`——放在 `.setup` 之前是为了 webview 起不来时也能留下版本痕迹。git 取值失败（源码包 / 无 `.git`）降级 `unknown` 而非 panic；`build.rs` 声明 `rerun-if-changed=../.git/logs/HEAD`，提交后下次编译自动刷新提交号。附 `scripts/test_build_info.js` 15 条回归断言（注入点、降级、日志位置与顺序）

- **手动暂停监控**——托盘菜单新增「暂停监控 / 恢复监控」，点击即全局暂停：新增 `pause.rs` 状态机（`MANUAL` 原子位），键鼠活动、前台应用、媒体播放三处入口统一 `is_paused()` 门控，暂停期间各守卫点直接丢弃未入账增量、恢复后不补记暂停时长——「暂停」就是钱先冻结。暂停瞬间徽章切换为「已暂停 · 钱先冻结」，托盘菜单文案随状态切换；重启后默认恢复监控（状态不落盘，避免忘了在暂停、一整天没数据）

- **久坐提醒**——连续活跃超过阈值（设置页可调，默认 50 分钟，钳制 1–120）时提醒起来活动。判定在 `remind.rs` 分钟级 tick：连续活跃按「无 5 分钟以上键鼠空闲」计，起身倒水即重置起算点；提醒后进入 5 分钟冷却，不连发轰炸。提醒统一走系统通知

- **下班提醒**——工作日 + 已过下班时间（`pm_end`）+ 当日有监控记录时，系统通知「到点了，下班吧牛马 · 今天已赚 ¥X」，把当天实时进账直接递到眼前。当天只提醒一次；程序在过点后才启动也能补弹（首拍即满足全部条件，无需特殊分支）

- **全局快捷键**——Alt+Shift+N 显隐主窗（可见时隐藏、隐藏时唤起并聚焦）、Alt+Shift+P 暂停/恢复监控，基于 tauri-plugin-global-shortcut，仅在按下沿触发；注册失败只记 debug 日志，不影响启动。设置页新增「守护」卡集中管理：久坐提醒开关、连续活跃阈值、下班提醒开关、全局快捷键开关（关闭即注销全部已注册快捷键，与其他软件冲突时可关掉），变更即时保存。所有新配置字段带 serde 默认值（开关默认开、阈值默认 50），旧 config.json 无需迁移

- **设置页信息架构重组**——拆解「基础配置」大杂烩，重组为六张语义化卡片（薪资与作息 / 加班 / 守护 / 数据监控 / 外观 / 系统与数据）；五处长说明收进「详情」原生折叠（零 JS 依赖）；久坐阈值下限放宽到 1 分钟（原 30）。纯结构重排：全部元素 ID 不变、app.js 零适配，8 个设置页相关测试脚本 370 断言全绿

- **调试模式（彩蛋）**——2 秒内连点侧栏「设置」按钮 5 次，设置页最底出现调试卡（再连点 5 次隐藏）。卡片为终端控制台风格（monospace + 终端绿 + 标题栏），与业务卡一眼区隔，含五个测试按钮：**下班提醒 / 休息提醒**直发对应真实文案的系统通知，一键验证通知通道与文案；**重置提醒状态**清空下班「今天已提醒」标记与久坐冷却/计时；**立即调度**按真实规则跑一次提醒判定（与后台 60 秒自动调度同一函数）；**模拟久坐并触发**把「连续活跃起点」前拨到阈值之前再跑真实调度——久坐真实链路要求「连续活跃 ≥ 阈值」，阈值最小 1 分钟、tick 又是 60 秒一拍，手工复测至少要等一拍且测不到 50 分钟档的文案，该按钮只伪造「起点」这一项运行时状态（外加清冷却），判定函数零特判，秒级走完判定 / 文案 / 通知 / 状态重置全链路。下班提醒每天只触发一次，配合「重置 + 立即调度」即可当天反复复测真实链路。文案提取为 `offwork_texts` / `sedentary_texts`，真实触发与调试按钮同源不漂移；按钮点击有 toast 反馈（区分「命令没发出去」与「发出去了系统没弹」），失败落 debug.log。进程级开关不落盘，重启后默认关闭，不占任何 UI 开关与配置字段

### 修复

- **调试卡「已开启却看不见」**——调试卡是限高设置页（`.app` 纵向 flex）的子项且带 `overflow:hidden`，这会让 flex 子项的 `min-height:auto` 退化为 0：设置页内容溢出时它成为唯一可压缩项、被挤成 0 高度，于是 `hidden` 类已移除、DOM 也在，视觉上却完全不可见（业务卡 `.card` 因 `overflow` 默认 `visible` 而免疫）。给 `.debug-card` 加 `flex-shrink:0` 保住内容高度，并在开启时 `scrollIntoView({block:"nearest"})` 滚入视野（卡片位于长设置页最底部，仅显隐不足以被看到）。附两条前端回归断言，防止将来把 `flex-shrink` 当冗余「优化」掉

- **绿色分发的系统通知通道**——exe 不经安装器放到任意目录（如 `Tools\`）运行时，tauri-plugin-notification 判定「已安装」、以未注册的 AppUserModelID 发 toast，Windows 静默丢弃且插件吞掉真实错误，通知永远不弹。现启动时自动向 HKCU 注册 AUMID：键名用应用 identifier（与安装目录无关）、每次启动覆盖写，换目录后下次启动自动刷新，任何人解压到任何位置首次启动即通；toast 图标用编译期内嵌的 ico 落盘到应用数据目录再指向它（WinRT 对 IconUri 指向 exe 的提取不稳定，实测显示空白）；通知投递失败与前端调试按钮命令失败同步解除静默，均落 debug.log。附 AUMID 注册读回与图标文件写出回归测试

## [1.2.0] - 2026-09-20

### 新增

- **数据洞察三视图：时段热力 / 多周趋势 / 身体账单**——周账单页从单一视图升级为四个洞察页（本周账单｜时段热力｜趋势｜身体账单），切换交互与明细翻页器一致（‹ 页名 › 循环 + 圆点直达 + 内容滚到头再滚滚轮翻页，`data-btab` 命名空间）；懒渲染契约：切页 / 翻周才 invoke，不进 tick 轮询，翻周联动刷新当前所在页。后端新增 `insights.rs` 三个聚合命令（`with_db` 锁内取配置快照、锁外查询）：**时段热力**（`get_hour_heatmap`）把活动 / 应用 / 播放三源小时表聚成 7×24 格，前端 div grid（零 canvas）按 `sqrt(v/max)` 归一到 5 档金色阶，hover 的 title 给出「键鼠 N 次 · 前台 X.Xh · 在响 X.Xh」，汇总行给一周峰值时段；**多周趋势**（`get_week_trend`）复用 `weekbill::week_bill` 逐日应赚保证口径严格一致，近 8 周每日进账（金实线）与摸鱼率（红细线）手写 SVG 双线——摸鱼率在前台总时长为 0 时记 null，**断线不画 0**（监控断了不等于摸鱼 100%）；每点挂竖带热区 + 浮层，未来周封顶不画；**身体账单**（`get_body_bill`）把一周的键鼠劳损折成身体消耗：点击（单击+右键+中键+侧键，双击折算 1 次，口径与周账单一致）、按键、滚轮、按 96dpi 估算的滑行距离（米），四指标卡 + 「最累的一天」+ 按天金色单柱（未来日不画）。事件数口径全链路统一：`moves+left+dbl+right+wheel+mid+xbtn+keys`，wheel_ticks 不计。新增 9 条 Rust 单测（只测纯函数，不碰真实文件库）与 `scripts/test_insights.js` 111 条前端断言（结构体契约、口径 SQL、SVG/heatLevel/fmtWan/fmtMeters 纯函数、$() 兜底）

- **远程加班排除开关 UI**——v1.1.0 的远程会话识别只有后端字段 `overtime_exclude_remote`（默认开），设置页没有开关可关。现在加班设置卡补上「远程会话期间不记加班」开关：load 回填（配置缺字段按默认开处理）、readCfg 采集、change 即时保存三处接线，落盘 `config.json`；关掉后远程会话不再跳过自动加班记录（判定逻辑不变，RDP 走 `SM_REMOTESESSION`、第三方走虚拟输入设备特征串）

## [1.1.0] - 2026-09-18

### 新增

- **远程会话识别（诊断先行）**——为根治「远程连接（RDP / 向日葵 / ToDesk / UU）导致加班被刷新」问题，先落地诊断能力以抓出远程虚拟输入设备的确切名字。关键认知：**第三方远程进程常驻 ≠ 远程会话**，必须用「活跃输入是否来自远程虚拟设备」判定，而非进程是否存在（否则开着 UU 没连也会被误判成远程，自动加班记录被永久关掉）。实现：`win.rs` 新增 `raw_input_hdevice` / `raw_input_device_name` / `list_raw_input_devices` / `dump_raw_devices` / `note_input_device`，封装 `RIDI_DEVICENAME` 设备名解析与 `GetRawInputDeviceList` 枚举；`activity.rs` 的 `WM_INPUT` 回调在每次输入时记录来源设备（首次记其名到 `debug.log`）；`tray.rs` 临时增加「调试：导出输入设备」菜单项触发 `dump_raw_devices`。下一步：依据 `debug.log` 里远程设备的特征名（RDP / Sunlogin / ToDesk / UU / Virtual / Mirror 等），实现实时远程会话判定并接入加班记录（远程期间跳过自动加班记录）

- **远程会话识别落地（接加班逻辑）**——把诊断阶段的设备名观测升级为实时判定并接入自动加班记录。新增 `src/remote.rs`：`is_rdp_session()` 走系统权威的 `GetSystemMetrics(SM_REMOTESESSION)`；`observe_input_device()` 在每次 `WM_INPUT` 时解析设备名、命中远程特征串（`RDP`/`RDP_KBD`/`RDP_MOU`/`SUNLOGIN`/`TODESK`/`UU`，大小写不敏感，刻意不含裸 `VIRTUAL`/`MIRROR` 以免误伤物理设备）即记最近远程输入时刻，靠 60s 衰减窗口覆盖「连着但没动」的 idle 期；`is_remote_active()` 聚合 RDP 与近期远程输入。配置新增 `overtime_exclude_remote`（默认开），`main::maybe_record_overtime_lock` 检测到远程会话时跳过自动加班记录（但标记已处理，避免远程结束后又把错误时间补记上）；手动补录入口不受影响，远程日仍可事后手填。`win.rs` 的 `note_input_device` 诊断函数已并入 `remote::observe_input_device`（其 debug 日志暂留，待 UU 真实设备名确认后移除），`dump_raw_devices` 等枚举函数保留供抓名。新增 3 条 Rust 单测（特征串命中 / 非物理误伤 / 衰减窗口边界）。临时诊断（托盘菜单「调试：导出输入设备」+ debug 日志）现已摘除（见下方「远程会话识别：临时诊断摘除」条目）

- **周账单**——侧栏新增第四个 tab「账单」，把一周的班、钱和摸鱼打成一张可翻页的账单。后端 `weekbill.rs` 一处聚合出七天明细（新增 `get_week_bill` 命令，入参 weekOffset 向前翻历史、本周封顶）：应赚工资沿用满勤推算、今日实时、摸鱼成本按逐日折算（Σ 当日摸鱼秒 / 3600 × 当日时薪，当日时薪 = 满勤 ÷ 当日工时，仅作展示、不进 total_income 减法），休息日只记加班费、未来工作日按 0 呈现。前端两种风格一键切换（设置页新增「账单风格」分段，存 `config.bill_style`）：**小票风**是张纸白锯齿边的小票——单号取周起日、虚线分隔的收支行、7 根金条按当日应赚归一、休/未到的日子给占位字、右下「已验讫」红章，顶部按周摸鱼率给四档金句（未配月薪时自动去掉提钱的那档）；**仪表盘风**是 KPI 大数字 + 环比箭头（上周为基，无基数不显）、2×2 迷你格、「最累的一天 / 最摸的一天」、应赚 vs 摸鱼成本双色双柱（各自归一）。监控关闭时工时与键鼠行给「—」，未配月薪时金额行整行隐藏但加班费、摸鱼成本照常。新增 10 条 Rust 单测（聚合口径、跨月、休息日、今日实时、监控关闭）与 `scripts/test_week_bill.js` 87 条前端断言

- **摸鱼成本货币化**——上一轮把应用分成了工作 / 摸鱼 / 沟通 / 其他四类，但只看时长没有痛感。现在主页 hero 卡在赚钱大数字下方加了一行「🐟 摸掉 ¥X · 摸鱼率 Y%」，摸鱼时长 × 时薪实时折算，和上方跳动的已赚金额形成「一边赚、一边烧」的对照；右侧按摸鱼率给出四档损味文案（<10%「老板的梦中情马」、10–25%「摸得克制，装得敬业」、25–40%「快三分之一的班白上了」、≥40%「老板看完连夜注销公司」）。应用明细「今日构成」四格也补上各类折算金额，摸鱼类红色高亮。口径：摸鱼率分母为被统计到的前台总时长（不含离开电脑的空档，午休开会不算摸）；休息日、未配月薪、当日无应用记录时烧钱行整块隐藏，摸鱼为 0 时显示 ¥0.00 不隐藏（正向激励）。纯前端实现，复用 `categories` 与状态时薪，**零 Rust、零新 IPC**；`scripts/test_slacking.js` 扩至 48 条断言（公式、三守卫、四档文案、$() 兜底）

- **摸鱼统计：应用分类（工作 / 摸鱼 / 沟通 / 其他）**——应用使用此前只有时长排行，看不出时间花在哪类事情上。现在每个应用归入四类之一：内置常见应用默认表精确匹配 + 用户覆盖（`config.app_categories`，展示名 → 分类，空串视同未覆盖）+ 未识别一律归「其他」；查询时归类，改分类**即时重新归类全部历史**，零迁移。后端 `AppUsageItem` 增 `category` 字段、`AppUsageSummary` 增 `categories` 四类切片（固定序 work/slack/comm/other，作为前端配色锚点），`summary()` 接收 `Config` 参与归类。前端：应用明细页新增「今日构成」卡（堆叠构成条 + 四格时长，点应用行的分类标签可循环改分类）、主页「今日监控」卡新增摸鱼速览行（工作 X · 摸鱼 Y）。顺带加固配置保存：`config` 写盘的全部失败路径记 debug.log——配置写不进去等于用户改动静默丢失，绝不允许无声无息。新增 `scripts/test_slacking.js` 17 条断言与 3 条 Rust 单测（默认表优先级 / 固定序折叠 / 空表）

- **存储占用总览与分类细分**——「数据存储」卡片原来只平铺一行「主库 / 写前日志 / 图标」三个数字，看不出到底是谁在占空间。现在给出**总计**与**九类细分**（加班记录、键鼠活动、应用使用、媒体播放、索引与空闲页、写前日志、图标缓存、配置与节假日、运行日志），每项显示大小、占比与行数，并按占用降序排列，顶部用一条堆叠色条直观呈现谁占大头。表级占用用 SQLite 的 `dbstat` 虚拟表精确统计（含索引与溢出页；rusqlite `bundled` 已编译该模块），同一业务的两张表（日汇总 + 逐小时明细）合并为一行——用户关心的是「键鼠活动占多少」而不是 `act_hourly` 占多少。dbstat 不可用时自动退化为按行数比例估算并在界面标注「估算」。真实库实测：应用使用 64KB/1434 行、键鼠活动 36KB/899 行、媒体播放 20KB/347 行、加班 4KB/25 行、写前日志 4.0MB（整理后归零）。新增 7 条 Rust 单测（分组求和、other 防下溢、总计口径、降序、key 覆盖、dbstat 真实值、估算分摊）与 41 条前端断言（`scripts/test_storage.js`）

- **跨午夜加班归属**——修复「通宵加班一分钱不算」的静默丢账。此前加班归属日取的是**检测时刻**的日期，而锁屏（=下班离开）每 5 秒才检测一次：9/11 加班到 9/12 凌晨 1:30 锁屏，会被算成 9/12 的加班，于是 `lock_min(90) <= ot_start(1080)` 直接判定无加班——9/11 和 9/12 都没有记录，且毫无提示；另一种情况是 23:59:58 锁屏、00:00:02 才检测到，整段加班被错记到第二天。现在归属日**由锁屏时刻自己决定**：早于 06:00 的离开视为熬过午夜，归属前一天且结束时刻按 +24h 计算（9/12 01:30 → 记在 9/11，7.5 小时）。明细页对这类记录显示「次日 01:30」，手动补录表单新增「下班时间在次日凌晨」勾选。数据库新增 `cross_midnight` 列，并补上**幂等加列迁移**（`pragma_table_info` 查到缺列才 `ALTER`）——v1.0.0 已有真实用户库，不能靠删库重建，而无条件 ALTER 会让全新库报 duplicate column。新增 8 条单测，含一条固定旧行为（`old_behavior_cross_midnight_yielded_nothing`）以说明修复了什么

- **数据存储与生命周期管理**——设置页新增「数据存储」卡片，显示主库 / 写前日志 / 图标缓存的占用与最早数据日期，可设保留期（默认**永久保留**）并一键「立即整理」。核心是解决 **WAL 常年不收缩**：SQLite 的自动 checkpoint 只复用空间不缩文件，只有最后一个连接关闭时才 truncate，而托盘程序全程持连接不退出，实测 WAL 停在 4.06MB、是主库 248KB 的 16 倍；必须显式 `PRAGMA wal_checkpoint(TRUNCATE)` 才归零（同一份数据实测 PASSIVE 后仍是 4.4MB，TRUNCATE 后为 0）。现在每天首次启动自动整理一次。配套：图标缓存超过 180 天自动清理（可从 exe 重新提取，不损失数据）、删除 8-21 迁移遗留的 `overtime.json.legacy.bak`。**默认不删任何历史数据**——保留期为 0 时不清理，该规则由 `cutoff_for` 单独承载并有单测断言，避免默认值一旦改动就静默丢失老用户数据

- **休息日 / 节假日加班**——设置页原「周末加班」开关此前形同虚设：`main.rs` 在加班计算前就有 `if !is_workday { return }`，非工作日根本走不到计算，开关里层的判断是死代码；且休息日沿用工作日的 18:00 起算，上午来、下午 5 点走的人会因 `lock_min <= ot_start` 一分钱算不到且毫无提示。现在：日期类型接入 `holiday` 数据（0 工作日 / 1 周末 / 2 补班 / 3 法定节假日，无数据时降级按周几），休息日与法定节假日用**独立的起算时间**（默认 09:00）与**独立费率**（未配置时逐级回退：法定节假日 → 休息日 → 工作日），调休补班日按工作日规则计算、不受开关影响。设置页新增「休息日加班起算时间」「休息日费率」「法定节假日费率」三项，开关关闭时自动隐藏。新增 8 条单测覆盖三档判定、费率回退链与开关行为

- **活动 / 应用使用 / 媒体播放支持按日期回看**——三个二级页顶部新增「前一天 / 后一天」，可翻看任意历史日期（此前写死今天，库里 12 天的活动数据只能看当天）。标题随日期变（今日 / 昨日 / 9月8日），柱状图在历史日期不再标「当前小时」金色柱，空态文案也不再说「今日暂无」。未来日期翻不过去。主界面卡片固定反映今天：返回主界面时日期自动复位，不会被历史数据顶掉

- **加班明细支持月份切换**——明细页新增「上一月 / 下一月」，可回看任意历史月份（此前写死当月，8 月的记录查不到）。明细页顶部显示所选月份小计；主界面「加班总览」固定反映当月，翻到历史月份不会被历史数据顶掉。同时放开补录限制：历史月份也能手动增删改（此前后端硬校验「只能操作当月」，忘记的加班没法补记），仅保留「不能录未来日期」

- **首页副标题可选**——设置页新增「首页副标题文案」，7 种可选：动态状态（默认，随未开工 / 午休 / 快下班 30 分钟内 / 已下班 / 休息日自动切换）、明码标价、每一秒钱都在涨、搬砖的每一分钟都算数、原版、不显示、自定义（最多 40 字，输入时实时预览，留空回退动态句）。配置存 `config.json` 的 `tagline_style` / `tagline_custom`，两个字段都带 serde 默认值，旧配置文件不受影响

### 修复

- **悬停卡片大字金额消失 + 时间轴竖排成「多条进度条」**——用户先后两次反馈「大字今日已赚不显示 / 变成很小」。① 金额消失的**真根因**是 flex 收缩：`.card` 是 flex 列容器，`.hero` 是其中唯一带 `overflow:hidden` 的子项（为裁 `sheen` 光泽而加），按 CSS Flexbox 规范，flex item 的 `overflow` 一旦不是 `visible`，其自动最小尺寸就退化为 0——于是卡片内容只要超高，全部溢出量便由 `.hero` 独自吸收压扁（其它块 `min-height:auto` 等于内容高度，压不动），34px 金额被裁得只剩约 1px。窗口此前按「卡片 300px」配成 316，而按真实渲染逐块量算需要约 328px（head 17 + 金额 34 + 语录 14 + 进度条 20 + 预告 14 + 时间轴 23 + 四格 90 + 战果行 29 + 徽章 15 + 8 个 6px 间隙 48 + 卡片内边距 22 + 边框 2），即少了约 28~34px。修法两条并行：`.hero` 加 `flex: 0 0 auto` 彻底退出收缩（金额永不再被挤没，这是治本的护栏）；窗口高度 316 → 352（`tray.rs` 的 `HOVER_CARD_H` 与 `hover_card.html` 的 `body{height}` 必须同步，两侧一致性已写进注释与测试）。同时把金额从 `background-clip:text` 渐变裁剪改为纯金实心 `color:#ffd650` + 轻微阴影——透明 WebView 上的裁剪渐变文字本就不可靠，属顺手加固（主窗口不透明所以一直正常）。② 今日时间轴的 5 段（上午已/上午待/午休/下午已/下午待）本应横向排成同一条 8px 时间轴，却因 `.taxis` 缺 `display:flex` 而竖向堆叠成「好几条进度条」，让人以为有多个进度条且不知含义；已补 `display:flex` + `flex-shrink:0`（橙渐变=已搬完的时段、透明=待搬、斜纹=午休、白指针=现在，下方 09:00/12:00/13:30/18:00 四格刻度）。③ 缓存隐患：悬停卡 URL 的 `?v=h2` 硬编码在 `tray.rs`，不参与 `build.rs` 内容戳，改了 hover_card.html 内容 WebView2 仍可能读旧缓存；已把 `src/tray.rs` 纳入 `build.rs` 的 `TARGETS`。新增 `scripts/test_hover_card.js` 26 条断言：锁两条 CSS 契约（时间轴 flex、金额块不可收缩、纯金实心色）、两侧尺寸一致性（`body height == HOVER_CARD_H`）、以及「窗口高度必须 >= 344px 才装得下全部内容」的余量下限（已做负向自测：去掉 flex / 改回渐变裁剪 / 把高度调回 316 立刻红）

- **悬停卡片日期行去掉「第几天」计数器**——用户反馈「第2天」语义弱且易混淆：它数的是自然日而非工作日（周末也 +1），且计数器首日用 `nc_firstDay` 存在悬停卡独立的 WebView `localStorage` 里，与软件首装日不一致。改为日期行只显示「日期·周几」（`9月15日 周二` 样式）。相关回归守卫已并入 `scripts/test_hover_card.js`：不再引用 `nc_firstDay` / `dayN` / 拼接「第N天」，仍校验星期拼接

- **账单页三块面板同时渲染、底部露出未填充的仪表盘骨架**——`#billEmpty` / `#billReceipt` / `#billDash` 三个面板在 HTML 里都写了 `class="... hidden"`，但 `styles.css` 里从没给它们配 `.xxx.hidden { display:none }`，也没有通用 `.hidden` 工具类；而 `.bill-dash` / `.bill-empty` 自身带 `display:flex`，`hidden` 类因此彻底失效——账单页三块同时渲染：顶部在**有数据时也误显**「这一周还没有打工记录」空态文案，底部露出当前风格未渲染的那块骨架（`¥0.00` + 四个默认「—」的空卡片）。修法是在样式表加一条通用 `.hidden { display: none !important }` 工具类（`!important` 用于压过 `.bill-dash` 这类自带 `display` 的形态规则），一次覆盖全部以 `classList` 切换 `hidden` 的元素。另新增 `scripts/test_hidden.js`：扫描 `index.html` 里所有初始带 `hidden` 的元素，要求 `styles.css` 必须存在能隐藏它们的规则，并反向校验 `app.js` 切换的 id 在 HTML 中存在（已做负向自测：去掉通用规则后立刻点名 `#billEmpty`/`#billReceipt`/`#billDash`）

- **存储卡片「共占用」恒为 0 B、每行占比恒为 0%**——后端结构体是 snake_case 序列化（Tauri 不会把返回结构体的字段转成驼峰，项目里加班记录的 `cross_midnight` 也是如此），前端却按 `info.totalBytes` / `info.earliestDate` 取值，拿到的全是 `undefined`：总计显示 0 B，占比全部 0%（条形图只剩 `min-width` 撑出的几条彩色细线），「最早数据」那行也不显示。已改为 snake_case 取值。**根因是前端测试的桩数据也跟着写了驼峰**，等于自证清白：现已把 `scripts/test_storage.js` 的载荷字段与 `maintain.rs` 的 `StorageInfo` 逐字比对，并反向扫描 `renderStorageInfo` 读到的每个字段必须在结构体里存在（已做负向自测：改回驼峰立刻红，并直接点名 `totalBytes` 这个幽灵字段）；Rust 侧另加一条测试断言序列化后的 JSON 键与字段名一致，防止有人加 `#[serde(rename_all = "camelCase")]` 再把前端打懵

- **存储卡片与「立即整理」按钮报 `not allowed by ACL`**——新增的 `get_storage_info` / `run_maintenance` 两个命令没有登记进 Tauri 权限清单：命令必须列进 `build.rs` 的 `AppManifest::commands(...)`，tauri-build 才会为它生成 `permissions/autogenerated/*.toml`，进而才能在 `capabilities/default.json` 里写 `allow-*`。此前这份名单是**手工维护**的，漏一个的后果很隐蔽——编译照过、后端调度器内部调用也正常（内部调用不走 IPC，不受 ACL 限制），只有前端点按钮时才报「整理失败」。现已改为**从 `src/main.rs` 解析 `#[tauri::command]` 作为单一真相源**：名单自动生成，`capabilities` 里缺的 `allow-*` 由 `build.rs` 自动补齐（幂等，只在缺失时写盘并给出编译期提示）。另新增 `scripts/test_commands.js`，比对「后端命令 ↔ 权限登记 ↔ 前端 invoke」三方是否一致，以后漏登记会在测试里红

- **导出 CSV 点击「没反应」实为缺反馈、非机制故障**——周账单 / 加班明细的「导出 CSV」按钮点击后用户**看不到任何提示**，误以为没生效于是反复点，刷出一堆同名文件。经实测核实：本项目里 Tauri/WebView2 **并未**取消 `<a download>`（用户确认文件实际已落到下载目录），此前「WebView2 默认取消下载」的判断是错的。修法：导出不再用阻塞式 `alert`，改为点击后按钮显示「导出中…」并短暂禁用、成功后用内联状态条提示保存的文件名、并加 1.5s 冷却防止重复导出；底层仍走后端 `export_csv` 命令写盘到「下载」目录（文件名取 basename 并剔除非法字符防路径遍历），BOM 由后端写入（Excel 双击中文不乱码），`build.rs` 自动补齐 `allow-export-csv` 权限

### 优化
- **编译发布链脚本加固**——build.bat / release.bat / publish_release.py 三处系统性收紧。**build.bat**：新增 `test` 分支（cargo test + 新增 `scripts/run_all.js` 以子进程隔离逐个跑 15 个前端断言脚本、遇错即停——各脚本会改写 global.Date/global.$，共享进程必互相污染）；参数改为白名单校验，拼错不再静默不构建。**公共部分抽取**：与 release.bat 重复的 ROOT/路径、cargo PATH 兜底（旧终端窗口 PATH 快照问题）、镜像重试参数统一抽到根目录 `common.bat`，两脚本 `call` 一次即得、一处维护。**release.bat**：发布流程第 0 步先跑上述测试；非 main 分支直接警告（/y 模式拒绝、交互模式二次确认），push 目标从写死的 main 改为当前分支，tag 与分支不再脱节；`git add -A` 前先打印 `git status --porcelain` 清单，防止杂文件被静默提交；版本号同步后重新解析 Cargo.toml / tauri.conf.json 精确比对（原只查字符串出现过）；同版本重发时远程 tag 冲突给出 `push --force` 指引；`--generate-notes-only` 调用补上遗漏的 `--repo`。**publish_release.py**：TLS 证书校验只在走本地 Clash 代理时降级（直连保持严格校验）；CHANGELOG 找不到对应版本段时回退 `[未发布]` 段并告警（原先静默产出空变更说明）；发布产物生成 SHA256SUMS.txt 一并上传、Release Notes 下载表增加短哈希列（原 hashlib 导入是死代码）；资产上传失败自动重试一次；代理端口与 target_commitish 支持环境变量覆盖（`NIUMA_PROXY` / `NIUMA_DEFAULT_BRANCH`）
- **周账单 / 加班明细支持导出 CSV**——两个数据最丰富的视图各加一个「导出 CSV」按钮（加班明细在页头右上、周账单在周导航下方右对齐工具条）。点击后走现有顶部居中 toast 弹窗提示保存的文件名（成功绿 / 失败红），并加并发守卫防连点重复导出——不再用内联状态条与「导出中…」按钮态，统一复用项目的消息弹窗。数据本来就已拉到前端 JS，用后端命令 `export_csv` 写到「下载」目录（文件名自动清洗防路径遍历），Excel 直接双击中文不乱码。
加班明细 CSV：日期 / 是否跨午夜 / 锁屏或下班时刻 / 有效时长 / 加班费 / 餐补 / 合计 / 来源，按日期升序，末尾附「月度汇总」段（合计金额、总有效时长、加班天数）；
周账单 CSV：先「周汇总」段（周次 / 起止 / 总进账 / 基本工资 / 加班费 / 摸鱼成本 / 出勤工时与天数 / 键鼠点击次数），再「每日明细」段（日期 / 星期 / 是否工作日 / 有记录 / 当日工资 / 摸鱼秒数）。
CSV 字段走 RFC4180 转义（含逗号/引号/换行才包引号、内部引号翻倍）。
新增 scripts/test_export_csv.js 25 条断言锁住「两按钮存在 + 五函数存在 + 真实导出逻辑（转义/升序/空数据不下载/汇总/文件名）」
- **常驻线程命名**——给全部 9 个常驻/辅助线程加 `Builder::name`（niuma-scheduler / niuma-lock-monitor / niuma-activity-keyq / niuma-activity-raw / niuma-app-usage / niuma-audio / niuma-hover / niuma-focus-retry / niuma-startup-show），崩溃 dump 或调试器里一眼能认出是哪个线程在干活，不再全是匿名的 `<unnamed>`。纯治理、零行为变化；`Builder::spawn` 返回值用 `let _ =` 承接避免 must_use 警告。cargo check 零 warning、115 测试全过
- **远程会话识别：临时诊断摘除，判定策略定型**——依据实测日志收紧策略。用户「连出去控制另一台机器」（控制侧）时，本机注册的 28 个 Raw Input 设备里**零** RDP/向日葵/ToDesk/UU 特征设备——因为远程虚拟 HID 只在**被控侧**（别人连进本机）出现，控制侧抓不到对方虚拟设备是预期内，并非逻辑失效。关键价值：这 28 个设备里物理键鼠、`VIRTUAL_DIGITIZER`、系统虚拟化设备、某 `GVInput` 虚拟输入均**未被误判**为远程，`SIG` 名单（`RDP`/`RDP_KBD`/`RDP_MOU`/`SUNLOGIN`/`TODESK`/`UU`，刻意不含裸 `VIRTUAL`/`MIRROR`）对日常设备零误伤已验证。结论：RDP 走系统权威 `SM_REMOTESESSION` 稳覆盖；第三方靠 `SIG` 作 best-effort——控制侧完全不受影响，被控侧能否命中取决于对方虚拟设备真名，命中不了最多漏判、绝不误杀（绝不把正常加班的日子错当远程排除）。现摘除临时诊断：托盘菜单「调试：导出输入设备」、win.rs 的 `dump_raw_devices`/`list_raw_input_devices` 及 `GetRawInputDeviceList`/`RAWINPUTDEVICELIST`/`RID_DEVICE_INFO_TYPE` 连带清理、remote.rs 里 `[raw-device]` 调试日志；remote::observe_input_device 与 win.rs 的 raw_input_hdevice / raw_input_device_name 生产代码保留

- **加班明细「合计」展示升级为金色统计卡**——原来明细页顶部是一行不起眼的 12px 灰字「合计 ¥X · X 小时 · X 天」，金额不够突出。现在升级为与主页「本月加班战果」同语言的金色统计卡：左侧大号金色金额（¥X），右侧三格统计（有效时长 / 加班天数 / 日均进账），金卡描边与金色字沿用主题色 #ffd650；无数据时整卡退化为居中的「该月暂无加班记录」灰字（文案由「本月」改为更准确的「该月」，因为明细页可翻看任意历史月份）。纯前端实现，零 Rust、零新 IPC；新增 scripts/test_ot_summary.js 15 条断言锁住「金卡落地 + 三真实字段仍流入渲染 + 空态切换」

- **导航改版：顶部三 tab + 明细分段直达**——原方案是二级页顶部「‹ 返回」的钻入式导航（主界面卡片点「查看明细›」进入、返回按钮退出），想从加班明细跳去活动明细得退回主页再进。曾改试 Windows 11 侧边图标栏（6 项直达），但窗口因此 440→488 变宽、6 个 tab 对这个小工具也偏多。现改为**顶部导航条**（主页｜明细｜设置 3 个文字 tab，窗口宽度回到 440，设置右置、主页首位，均为原生惯例）+「明细」tab 下的**分段控件**（加班｜活动｜应用｜媒体），分段条只在明细视图出现，主界面 4 张卡片的「查看明细 ›」按钮保留并直达对应分段；「明细」tab 记忆上次浏览的明细页（默认加班）。实现上 4 个明细视图 DOM 原样保留、`curView` 语义不变，懒渲染守卫、返回主页日期复位、设置页自动保存三条契约零改动；新增 `scripts/test_topnav.js`（tab 顺序、分段覆盖、无残留引用、`$()` 兜底 21 条断言）

- **主页「今日实时」卡片升级 + 监控三合一 + 切换动效**——三处以原生 Windows 现代化 UI 为准绳的主界面打磨。**赚钱进度条**：「今日实时」卡内新增实时进度条（已赚 / 全天应赚），全天应赚 = 时薪 × 当日工时（复用后端已有字段，零 Rust 改动），休息日或未配置时薪自动隐藏，进度封顶 100%；**监控卡三合一**：主页原本活动 / 应用 / 媒体三张监控卡占大半屏，现合并为一张「今日监控」卡，卡头内嵌原生风格小分段控件（活动｜应用｜媒体）即时切换预览，「查看明细 ›」跟随当前分段直达对应二级页，主页一屏尽收核心信息；**视图切换动效**：视图切换增加 Fluent 式淡入（0.15s 上浮渐显），利用 `display:none` → 可见时 CSS animation 自动重放、无需 JS，`prefers-reduced-motion: reduce` 下自动关闭尊重系统设置。新增 `scripts/test_hero.js`（进度条守卫/封顶/隐藏分支、监控映射、旧按钮无残留、动效与兜底共 42 条断言）

- **存储细分的大小与占比对齐成列**——原来大小后面紧跟着占比、再接一个定宽的行数列，数字长短不一（`3.96 MB 0%` vs `4.0 KB 0%`）就参差不齐，看着乱。现在每行用 grid 固定列宽（大小 66px、占比 46px、行数 58px），三列各自右对齐并用等宽数字（`tabular-nums`），行与行之间严格对齐；图标缓存的计数单位从「个」改为「个文件」，不再让人猜是 74 个什么

- **前端缓存戳自动同步，不再需要手工改版本号**——WebView2 会缓存 `tauri.localhost` 下的 HTML/CSS/JS，仅重新嵌入 exe 不够，必须让资源 URL 变化才能穿透缓存，因此 `index.html` / `styles.css` / `app.js` 三处引用都要带 `?v=<指纹>`。此前这个值靠手工同步四处（漏一处改动就静默不生效，排查时看不出是代码没生效还是真有 bug）。现在 `build.rs` 用 frontend 目录的内容指纹自动生成并回写这四处：改完前端直接编译即可。关键点是**哈希前先归一化剔除版本号本身**，否则「回写版本号 → 内容变化 → 指纹变化 → 再回写」会形成死循环，导致每次编译都改版本号并强制重编整个 crate。顺带修正目录遍历未排序的问题（`read_dir` 顺序不保证，同一份内容可能算出不同指纹）

- **锁中毒收口**——新增 `sync::lock`，生产代码里 38 处裸 `.lock().unwrap()` 全部改为经它获取。常驻托盘程序中，任一线程在持锁期间 panic 会让该锁永久中毒，此后**每个**碰它的线程都会跟着 panic，一处小错即连锁拖垮整个托盘。现在中毒时记日志并自愈取回数据（本项目的临界区都只读写状态，不会留下半截结构）。含 2 条回归测试：持锁期间 panic 后仍能取回数据且值完好

- **前端按需渲染**——活动 / 应用使用 / 媒体播放三个模块此前每 2 秒无条件重绘全部 DOM，包括隐藏在主界面背后的二级页图表与长列表（约每轮上百个节点）。现在只渲染当前可见视图：主界面仅更新汇总卡片，二级页的图表与明细列表仅在对应视图打开时才绘制。切换视图时用缓存立即补画，不留空窗；在设置页停用某个监控会清空其缓存，切回主界面不会残留旧数字
- 三个 2 秒轮询错开 700ms 相位，消除同一时刻三连发 IPC 造成的卡顿尖峰
- 媒体播放补上图标缓存合并（此前遗漏，导致应用图标 base64 每 2 秒重复回传）

## [1.0.0] - 2026-09-10

首个正式版本。此前程序从未发布过，因此版本号由 `0.1.0` 直接定为 `1.0.0`，
以下为此前全部功能与修复的汇总。

### 功能

- **工资计时**——实时显示今日已赚金额、已工作时长、距下班时间、每分钟赚多少、距发薪日倒计时；跨天自动重置，不累计
- **托盘图标绘制金额**——金额直接画在托盘图标上，无需打开窗口
- **托盘悬停卡片**——彩色渐变卡片（设置页可开关），鼠标悬停托盘时显示；关闭时回退到系统原生 tooltip
- **加班追踪**——下班后锁屏自动记录离开时刻，计算加班费与饭补；明细页支持增删改
- **活动监控**——基于 Raw Input（`WM_INPUT`）采集鼠标键盘：点击、滚动、按键、移动距离，逐小时分桶并统计按键 Top 榜；不劫持输入法，无输入延迟
- **应用使用监控**——前台窗口钩子按应用计时（白名单机制，如微信），设置页可维护白名单
- **媒体播放监控**——音频会话峰值轮询，统计各应用的播放时长
- **节假日**——从 CDN 拉取国务院放假调休安排并本地缓存；断网时回退内置 2025/2026 节假日表
- **开机自启**——设置页开关，底层使用官方 `tauri-plugin-autostart`（Windows 写入当前用户的 Run 注册表键，无需管理员权限）
- **本地持久化**——加班、活动、应用、媒体历史全部存入 SQLite（WAL 模式），配置与节假日缓存仍为 JSON
- **单实例**——再次启动会聚焦已运行的窗口，而非开第二个
- **时长格式可配置**——`hms`（X时X分X秒）/ `hm`（X时X分）/ `h`（十进制小时）

### 修复（择要）

- 悬停卡片「鼠标移走后不消失」与「移到托盘不显示」两个偶发故障（同源：过度依赖 `cursor_position()` 做显示/隐藏裁决）
- Raw Input 键盘事件被静默丢弃，导致按键次数恒为 0
- 应用使用时长改用单调时钟，免疫系统时间回拨导致时长停记；段时长按整点/跨天边界正确分桶
- 未知 exe 的兜底显示名误取扩展名，界面显示成「exe」
- 配置保存改为合并保存，根治未传字段被静默重置
- 手动修改过的加班记录不再被自动锁屏数据覆盖
- 关闭活动监控时真正卸载钩子；休息日不计时、不计薪

### 工程

- 周期任务合并到统一调度线程；DB 访问统一走 `with_db`（锁中毒自愈 + 错误上报）；活动落盘改为增量写
- Win32 调用收口到 `win.rs` 平台层（RAII 守卫消除句柄泄漏），`main.rs` 的 `unsafe` 归零
- 发布 exe 静态链接 MSVC CRT，唯一外部依赖是 WebView2；启动崩溃落 `/panic.log`

### 已知限制

- **周末加班**（`weekend_overtime`）配置已预留，但尚未实现
- 节假日内置表仅含 2025/2026；未收录的年份在断网时会退化为按周一至周五估算

[1.0.0]: https://github.com/wangdxnum1/niuma-timer/releases/tag/v1.0.0
[1.1.0]: https://github.com/wangdxnum1/niuma-timer/releases/tag/v1.1.0
