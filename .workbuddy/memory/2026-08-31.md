# 2026-08-31

- 通读项目代码，建立整体认知。架构见 MEMORY.md。
- 关键结论：实现已远超 2026-08-18 设计文档（加班/活动/应用/媒体监控 + SQLite + 悬停卡片均已落地）；README 仍停留在初版需更新；根目录有若干调试脚本不应进发布物。
- git 最新提交 d6c6e59（8/18）后有一批未提交改动（frontend 三件套 + config/main/overtime/tray.rs/Cargo.toml/build.rs/capabilities）+ 未跟踪诊断脚本，属活跃 WIP。
- 完成三步收尾：①重写 README.md/README.zh-CN.md 对齐 14 模块现状；②用 Win32 DeleteFileW 清理根目录诊断脚本（activate_wechat.py、diag_*.py、sim_input.py、diag_win_*.png、diag_audio.log、logs/）；③本地提交 8bc62ff（26 文件，3129 增/214 删）。未推送（按约定用户自推）。.workbuddy/ 保持未跟踪未入库。
- 修痛点#1：main.rs 末尾 `[frontend-v3]` 注释实为过时误导（build.rs 已用 TAURI_FRONTEND_FP 指纹自动重编前端），删去并改为准确说明；同步更正 MEMORY.md 三条过时约定。将随本次单独提交（一次改一个痛点）。
- 修痛点#2（监控套件 Win32 维护成本，第一步）：新建 src-tauri/src/win.rs 作为共享 Win32 原语归属地，把 activity/app_usage/lock_monitor 三处重复的 GetMessageW 消息循环收敛为单个 run_message_loop()，并写入「钩子/事件回调线程须跑消息循环」安全不变量文档。提交 32ef377。未编译验证（沙箱 cargo 失败），需用户在本地 cargo build 确认。托盘悬停卡片看门狗/全局钩子等更凶险耦合本回合未动。
- 深化痛点#2（钩子生命周期 RAII 化）：win.rs 新增 LowLevelHook 守卫（持有 HHOOK，Drop 自动 UnhookWindowsHookEx）；重构 activity.rs::hook_thread 为「鼠标/键盘分别安装，任一失败即提前 return、已成功的钩子随作用域退出自动卸载」，修掉原 match 错误分支「装鼠标成功、装键盘失败漏卸鼠标钩子」的残留泄漏。SetWindowsHookExW 的 hmod 形参为 Option<HINSTANCE>（已查证 windows-0.61.3 签名），故传 None。提交 206c7d3。仍沙箱未编译，需本地 cargo build 确认。注：app_usage(SetWinEventHook/HWINEVENTHOOK) 与 lock_monitor(WTS) 用不同句柄类型，不在本守卫覆盖范围内，留作后续。
- 深化痛点#2 编译修复：本地 build.bat 报 E0133（UnhookWindowsHookEx 是 unsafe fn，需在 uninstall 内用 unsafe{} 包裹）+ 两个 unused variable 警告（mouse/keyboard 改 _mouse/_keyboard）。修复后用户本地编译通过，已 amend 入 206c7d3 → 新 hash 8255c22。
- 实现痛点#3（周末加班预留未实现）：config.weekend_overtime 此前是死字段，且前端 app.js readCfg 硬编码 `weekend_overtime: false` 会覆盖已存配置（真 bug）。改动：overtime.rs compute_record 周六/周日且开关关→返回 None（自动锁屏路径生效）、save_manual 周末未开开关→明确错误；config.rs 注释更新；前端 index.html 加「周末加班」开关、app.js 回填+读取改 checkbox+change→saveNow。提交 1d690bb。未编译验证（沙箱 cargo 失败），需本地 build.bat 确认。边界：按自然周几判断，调休补班日暂按周末处理（加班计算未接入 holiday 日类型）。
- 新优化（非原痛点列表）：锁屏后暂停时间类统计。lock_monitor 新增全局 AWAY 状态（WTS_SESSION_LOCK→true / UNLOCK→false）+ is_away()；app_usage.tick() 与 activity 鼠标/键盘钩子回调在 AWAY 时跳过（避免锁屏界面/解锁输入污染应用使用与活动数据，且解锁后无时间跳变）；audio_usage 媒体播放保持原样（听歌锁屏后继续统计，符合需求）。提交 d0dd746。以「锁屏」事件为离开信号；纯息屏未锁屏场景若需覆盖要引入 windows Power 电源通知（RegisterPowerSettingNotification + GUID_MONITOR_POWER_STATUS，需加 cargo "Power" feature），留作后续。用户已确认「锁屏这版就够用」。
- 实现痛点#6（SQLite 迁移版本管理）：db.rs 原 conn() 无条件 execute_batch(SCHEMA) 建表（无 user_version 概念）。新增 CURRENT_DB_VERSION=1 常量 + migrate_db()：启动读 PRAGMA user_version，按版本差逐步迁移后写回版本号；v0→v1 建全部表（IF NOT EXISTS 幂等），未来加列只需在 `if version < N` 分支追加 ALTER。conn() 改为调用 migrate_db()。提交 6d30125。未编译验证，需本地 build.bat。
- 实现痛点#7（拆分单体 1s 主循环）：main.rs 把原单线程 1s 循环里的三件事抽成 free fn——refresh_tray()（get_status+update_tray 托盘刷新）、maybe_rollover_day()（last_date 变化触发节假日刷新）、maybe_record_overtime_lock()（let-else 取锁屏时间戳→判已见/工作日→calc_record+upsert_record）。原循环替换为两个线程：①1s UI 刷新线程（仅 refresh_tray）；②5s 业务轮询线程（rollover_day + record_overtime_lock）。副作用：锁屏加班记录延迟由 ≤1s 放宽到 ≤5s，对加班统计无影响。提交 1769293。未编译验证（沙箱 cargo 失败），需用户本地 build.bat 确认。
- 实现痛点#5（清理散落诊断代码）：删除 db.rs conn() 内 4 行 `[diag] conn:` 生命周期噪音打印、main.rs setup 内 3 行 `[diag] setup:` 噪音打印；audio_usage.rs summary() 移除 CALLS/call_no 调试计数与每 150 次写 debug.log 的散落诊断，并去掉仅服务于此的 AtomicUsize 导入。保留 db::debug_log 函数 + write_debug_log 命令（前端 app.js 仍用它做现场排障落盘）、所有 genuine error 与一次性迁移信息打印。根目录诊断脚本（activate_wechat.py/diag_*.py/sim_input.py/logs）经确认早已在 8bc62ff 清理，项目布局快照陈旧，无需再动。提交 5bfca21（3 文件，+1/−25）。沙箱未编译，需本地 build.bat 验证。
- 实现痛点#4（calc/overtime 单元测试）：在 calc.rs 与 overtime.rs 末尾各加 `#[cfg(test)] mod tests`，覆盖纯函数（无 DB/无 Windows 依赖）：calc 的 format_duration/to_min/overlap/daily_hours/days_to_payday/compute（含发薪日钳月末、工作日/休息日门控）；overtime 的 calc_valid_hours(核心向下取 0.5h)/to_min/format_hm/parse_lock_datetime/compute_record/calc_record（含周末开关开/关、不足 1h 返回 None、自定义起算、默认回退 pm_end）。测试用 Local.from_local_datetime 构造时刻、Config::default() 构造配置。提交 887957e（2 文件，+225）。沙箱无法 cargo test，需用户本地 `cargo test` 验证；未跑过，若编译/断言有误需据本地输出回修。
- 实现痛点#8（应用使用白名单可在设置页维护）：config 新增 app_whitelist_enabled(bool 默认 false)+app_whitelist(Vec<String> 默认空，serde default 兼容旧配置)；app_usage 新增 WHITELIST_ENABLED/WHITELIST 全局与 set_whitelist()，update_cur_app 按展示名(大小写不敏感)过滤非名单内应用（CUR_APP 置空、tick 不计）；main.rs apply_monitor_switches(启动 setup 与保存均调用) 内同步 set_whitelist；前端设置页加「仅统计白名单内应用」开关 + 应用名列表编辑器(输入/添加/可删 chip、去重)、readCfg/saveNow 联动、缓存戳 v10→v11。语义：关闭或空名单=统计全部(兼容现状)，开启且非空=只统计名单内。提交 6a59485（6 文件，+206/−3）。沙箱未编译，需本地 build.bat 验证。至此原痛点清单 #1–#8 全部落地（#9 沙箱构建跳过）：#1 frontend-v3 注释、#2 钩子 RAII、#3 周末加班、#4 单测、#5 诊断清理、#6 SQLite 迁移、#7 主循环拆分、#8 白名单。
- 新需求（非原痛点）：加班开关关闭时停止统计 + 前端不展示 + 保留历史数据。经排查后端已在痛点#7 的 maybe_record_overtime_lock 按 cfg.overtime_enabled 提前返回（关→不自动落盘），无需改后端。本次只改前端：index.html 给「加班总览」卡片加 id=otCard、缓存戳 v11→v12；app.js 新增 applyOvertimeVisibility(enabled) 控制卡片 display，load() 与开关 change 监听即时调用，loadOvertime() 在开关关时直接 return 跳过拉取/渲染（10s 轮询随之空转）。历史 ot_records 数据不受影响。提交 9193b52（2 文件，+17/−5）。沙箱未编译，需本地 build.bat 验证。注：tray 悬停卡片(hover_card.html)只显示实时工资/状态、不含加班，无需联动。
- 实现痛点 P4（图标提取移出前台切换/音频轮询热路径）：app_usage::update_cur_app 不再在 EVENT_SYSTEM_FOREGROUND 回调里同步 ensure_icon（GDI 取图标+PNG 编码+落盘会卡前台识别、偶发卡顿），改为：①后台 tick() 每 10s 对当前前台应用懒提取；②新增 APP_EXE_MAP(display→exe_path) 供 summary() 查询时补提取，覆盖「仅短暂前台、tick 来不及提取」的边界；CurApp 增 exe_path 字段。audio_usage::collect_playing 每 5s 轮询不再同步 ensure_icon，改为填充 PLAYING_EXE(display→exe_path)，summary() 查询时懒提取。两个 ensure_icon 调用点均移到非热路径，图标渲染与之前一致（盘缓存跨会话保留）。diff +32/−4（app_usage +22/−2、audio_usage +14/−2）。沙箱未编译，需本地 build.bat 验证。
- 实现 P10（键盘钩子回调瘦身：只采集入队，其余交工作线程）。背景：用户反馈优化后搜狗输入法延迟明显（原只是偶尔鼠标卡），根因是 WH_KEYBOARD_LL 挂在系统输入分发链上、每次按键都要等回调返回才下传给 IME；P9 修好鼠标钩子后鼠标不卡了，键盘钩子的拖累遂成为最显眼症状。改动（activity.rs）：新增 SPSC 环形队列（KEYQ_CAP=1024，掩码取模；pack_key 把 (vk,time) 打包进一个 u64，低 8 位 vk、高位 time），kb_proc 只 keyq_push 后立刻返回，回调内零原子读改写；note_input 打点、ENABLED 判断、50ms 去重、C_KEYS/key_pending 计数全部移入新线程 keyq_worker（每 20ms 批量排空）；队列满直接丢弃（统计宁可丢数据也绝不阻塞输入路径）；start() 起工作线程、shutdown() 先 sleep 40ms 排空再 flush_pending（否则最后一批按键会丢）。提交 116352a（1 文件 +104/−22）。沙箱未编译，需本地 build.bat 验证 + 搜狗输入法实测。**重要认知：回调体已极快（纳秒级），本次瘦身只能减少原子读改写，WH_KEYBOARD_LL 本身在输入分发链上这一根本拖累仍在**；彻底解法是 Raw Input（WM_INPUT 带外通道，不进钩子链）：win.rs 建 message-only 窗口 + RegisterRawInputDevices RAII，activity.rs 用 GetRawInputData(RID_INPUT) + RI_KEY_BREAK 过滤 + RAWINPUTHEADER.time 取时间戳替掉键盘钩子，逐键计数逻辑一行不改。等用户拍板是否实施。
- 实现 P3（托盘悬停卡片状态机重写为 actor 模型）：彻底消除痛点#2「更凶险耦合未动」的债务。原实现用 9 个全局 static（HIDE_PENDING/HOVER_CREATING/HOVER_PENDING_POS/WATCHDOG_RUNNING/HOVER_CARD_RECT/SHOW_PENDING/LAST_CLICK_MS/SHOW_TIMER_ACTIVE/TRAY_CENTER）+ 看门狗线程(150ms 轮询+2 次去抖) + 400ms 延迟显示计时器 + 1500ms 点击冷却 + 5 档 hover_show 重发 + hover_ready 握手，多线程并发读写同一批状态，难以推理。重写为：托盘事件/前端握手只向 mpsc 通道发 HoverMsg；单个工作线程独占 HoverController（含 Phase 枚举 Hidden/Pending/Shown），用 recv_timeout 统一驱动延迟显示/看门狗/淡出兜底/分档重发；空闲时阻塞 recv 无空转；状态变更仅在一个线程，结构上不可能数据竞争。前端 hover_card.html 协议与 update_tray 原样保留。行为等价：400ms 延迟显示、1500ms 点击冷却、Leave 不立即隐藏交看门狗去抖、单击即时隐藏、双击淡出开主界面。提交 99af506（amend 自 10e512e，+554/−425）。本地 build.bat 首报 E0614：RETRY_DELAYS 累加处对 Instant 误加解引用 `*base`（其实 `if let Some(base)=self.retry_base` 因 Instant:Copy 已按值绑定为 Instant），改为 `base` 后 amend 进同一提交。仍需用户本地 build.bat 最终确认。
