# 项目级 Cargo 配置
[env]
# Windows SDK 资源编译器（tauri-winres 嵌入 exe 图标/版本信息时需要）。
# 用带 target triple 的变量名，避免依赖 shell PATH，也不影响其他编译目标。
RC_x86_64-pc-windows-msvc = "C:\\Program Files (x86)\\Windows Kits\\10\\bin\\10.0.28000.0\\x64\\rc.exe"
