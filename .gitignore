target/
node_modules/
src-tauri/target/
bin/
*.user
build_*.log
# 运行期调试日志（debug_log 落在 %APPDATA%，从仓库根启动时会落到这里）
debug.log
# Trae 编辑器本地文件（不该入库）
.trae/
# Python
__pycache__/

# AI 助手工作记忆（本地产物，不该入库）
.workbuddy/
.superpowers/

# Tauri 编译生成物（tauri-build 每次构建重新生成，真相源是
# src/main.rs 的 #[tauri::command] 与 capabilities/default.json）。
# 随仓库跟踪会因 LF/CRLF 行尾在每次编译后产生无意义 diff。
src-tauri/gen/
src-tauri/permissions/autogenerated/
